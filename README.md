# The backend of the Social Media platform

## The tech stack is : FastAPI, uv

## Project requirements and libs are in : pyproject.toml file

### To run the app you need pip:

pip intsall uv
uv sync
uv run main.py